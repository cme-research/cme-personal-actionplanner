services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env.prod
    environment:
      - DJANGO_DEBUG=False
    volumes:
      - db_data:/app/data
      - media_data:/app/media
    restart: unless-stopped

volumes:
  db_data:
  media_data:
